version: '3.5'

services:
  app:
    build:
      context: .
    volumes:
      - ./:/var/www/
    networks:
      - coyote
    tty: true
    ports:
      - 8888:8888

networks:
  coyote:
    name: coyote